;; Engineering Hub — org-capture template reference
;;
;; These templates are now defined inline in $DOOMDIR/config.el.
;; This file is kept as a reference. Do NOT load it — doing so will
;; double-register the templates.
;;
;; If you prefer to load from this file instead, remove the
;; Engineering Hub section from your config.el first.
;;
;; Template keys:
;;   A   — prompt for agent type
;;   Ar  — @research
;;   Aw  — @technical-writer
;;   Av  — @technical-reviewer
;;   As  — @standards-checker
;;
;; Access via: SPC Shift+x (org-capture), then the key above.
;; Quick jump: SPC h e → today's Overnight Agent Tasks heading.
