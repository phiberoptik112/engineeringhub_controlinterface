"""Code-engineering integration: local repo registry and the Pi executor.

This package bridges the Engineering Hub agent system to the external Pi coding
agent (https://pi.dev), which runs an autonomous edit/run loop inside a local
git checkout. Phase 1 ships the project registry and a stubbed executor.
"""
